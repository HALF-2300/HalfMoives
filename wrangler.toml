# Cloudflare Pages (static frontend from Vite build)
# Use with: npx wrangler pages deploy dist --project-name=halfmovies
# Or in Dashboard: Build command = npm run build, Output directory = dist, Deploy command = (leave empty)
name = "halfmovies-pages"
compatibility_date = "2024-01-01"
pages_build_output_dir = "./dist"
